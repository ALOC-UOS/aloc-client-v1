import styled from '@emotion/styled';
import Animation from '@/styles/animation';

const CircularCourseLayout = styled.div`
  animation: ${Animation.FadeInScale} 2s;
  position: fixed;
  top: 50%;
  left: 100%;
`;

export default { CircularCourseLayout };
