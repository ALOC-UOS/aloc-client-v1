import styled from '@emotion/styled';

const APIManagerContainer = styled.div`
  display: flex;
  background-color: var(--color-white);
  padding: 12px 12px 24px 12px;
  border-radius : 12px;
  height: 100vh;
`;

export default { 
  APIManagerContainer,
};
